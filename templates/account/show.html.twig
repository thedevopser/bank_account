{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-2xl font-bold mb-4">Account Details</h1>

        <div class="mb-4">
            <h2 class="text-xl font-semibold">Account ID: {{ accountId }}</h2>
            <p class="text-gray-700">Balance: {{ balance | number_format(2, '.', ',') }} EUR</p>
        </div>

{% endblock %}
